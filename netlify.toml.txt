[build]
  functions = "netlify/functions"
  command = "npm install"

[[redirects]]
  from = "/api/pokemontcg/*"
  to = "/.netlify/functions/pokemon-proxy/:splat"
  status = 200

[functions]
  node_bundler = "esbuild"
  included_files = ["node_modules/**"]
  # Añadir un timeout más largo para las funciones
  timeout = 30 # segundos








